/*
#include <iostream>
#include <string>
#include "server.hpp"
#include "sensor.hpp"

using namespace std;

class Scheduler {
    public:
        Scheduler(); // Constructeur
        Scheduler(const Scheduler &scheduler); // Constructeur par recopie
        Scheduler(int nbrOfSensors, bool consolActivation, bool logActivation); // Constructeur par arguments
        ~Scheduler(); // Destructeur

        // METHODES
        void transmitter(std::string nom_capteur, int dataSens); // Réception des données du capteur
}
*/